- Compilar con:

```bash
make
```

- Correr con:
```bash
java -cp bin:lib/postgresql-42.2.5.jar app.App
```

- Limpiar con:

```bash
make clean
```

- Crear los contenedores con:

```bash
docker-compose up --build -d
```
